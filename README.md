# SCMT610\_GroupProject



Start work

git checkout dev

git pull --ff-only

git status



Checks - use before pushing

git branch --show-current

git remote -v



Do work → commit → push

git add -A

git commit -m "Describe change"

git push origin dev

